import java.time.LocalDate;
import java.util.List;

public class Stream extends Person {


	List roster = Person.createRoster();
	 
	 Integer totalAge = roster.stream().mapToInt(Person::getAge).sum();

}
